--Estas palabras suman una cantidad de un campo.
--Por ejemplo:

SELECT SUM(age) FROM users; --Este comando devuelve la cantidad total que suma el campo "age"

--tambien se puede hacer con resta e incluso un promedio

SELECT AVG(age) FROM users;

