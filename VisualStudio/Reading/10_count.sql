--Da la canntidad de entradas que tiene sobre el campo indicado 

SELECT COUNT(age) FROM users; -- devuelve la cantidad de usuarios que tienen registrada una edad

SELECET COUNT(email) FROM users; --devuelve la cantidad de usuarios que tienen registrado un email